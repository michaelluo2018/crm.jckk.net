create database jckk_crm character set utf8 collate utf8_general_ci;
use jckk_crm;
create table jckk_customer (
id int primary key auto_increment,
name varchar(100) comment "客户全称",
industry varchar(50) comment "行业",
customer_status_1 varchar(50) comment "客户状态1",
customer_status_2 varchar(50) comment "客户状态2",
company_nature varchar(20) comment "公司性质",
annual_turnover varchar(50) comment "年营业额",
contact_id varchar(50) comment "首要联系人",
note text comment "备注",
create_time int comment "加入时间"
)ENGINE=MYISAM DEFAULT CHARSET=utf8;
create table jckk_contact (
  id int primary key auto_increment,
  name varchar(50) comment "姓名",
  position  varchar(50) comment "职位",
  sex ENUM('0','1') comment "性别，0为男",
  mobile varchar(20) comment "电话",
  email varchar(50) comment "邮箱",
  qq varchar(20) comment "QQ",
  create_time int comment "加入时间"
  )ENGINE=MYISAM DEFAULT CHARSET=utf8;


  create table jckk_project (
    id int primary key auto_increment,
    customer_id int(10) comment "客户id",
    executor_uid int(10) comment "项目执行人",
    planning_uid int(10) comment "策划人",
    docking_uid int(10) comment "对接人",
    manage_uid int(10) comment "管理人",
    product_demand_1 varchar(50) comment "需求产品一级",
    product_demand_2 varchar(50) comment "需求产品二级",
    contract_status varchar(30) comment "合同状态 ",
    contract_amount varchar(30) comment "合同金额 ",
    payment_type varchar(50) comment "回款模式 ",
    payment_status varchar(50) comment "回款状态 ",
    rate varchar(50) comment "毛利率 ",
    cost varchar(50) comment "成本支出 ",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;



  create table jckk_user (
    uid int primary key auto_increment,
    chinese_name varchar(50) comment "中文名字",
    english_name varchar(50) comment "英文名字",
    sex ENUM('0','1') comment "性别,0为男",
    department_id  int  comment "所在部门 ",
    post_id  int  comment "岗位名称 ",
    address  varchar(200)  comment "地址 ",
    mobile  varchar(20)  comment "手机号 ",
    wechat  varchar(20)  comment "微信号 ",
    email  varchar(30)  comment "电子邮箱 ",
    qq  varchar(30)  comment "qq ",
    heard_img  varchar(100)  comment "头像 ",
    introduction  text  comment "个人简介 ",
    password  varchar(50)  comment "密码 ",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;


  create table jckk_department (
    id int primary key auto_increment,
    department_name varchar(50) comment "部门名称",
    sort  int(10)  comment "排序 ",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;



  create table jckk_post (
    id int primary key auto_increment,
    post_name varchar(50) comment "岗位名称",
    department_id  int(10)  comment "所属部门 ",
    pid  int(10) default 0   comment "上级post_id ",
    sort  int(10)    comment "排序",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;





  create table jckk_logs (
    id int primary key auto_increment,
    model  varchar(50) comment "模块",
    action varchar(50)  comment "方法 ",
    type int(10)  comment "类型，（增加 1，删除2，修改3） ",
    before_value  text    comment "之前值 ",
    after_value  text    comment "现值",
    title  varchar(100)    comment "标题",
    ip  varchar(30)    comment "ip",
    uid  int(10)    comment "操作人",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;




  create table jckk_setting (
    id int primary key auto_increment,
    sys_key  varchar(50) comment "键",
    sys_value varchar(200)  comment "值 ",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;


 

 create table jckk_feedback (
    id int primary key auto_increment,
    name  varchar(50) comment "用户填写姓名",
    email varchar(50)  comment "用户填写邮箱 ",
    title varchar(200)  comment "用户填写标题 ",
    content text  comment "用户填写内容 ",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;



 create table jckk_announcement (
    id int primary key auto_increment,
    title varchar(200)  comment "标题 ",
    content text  comment "公告内容 ",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;




 create table jckk_post_permission (
    id int primary key auto_increment,
    pid int comment "post_id",
    mid int comment "menu_id",
    add_operate int default 0 comment "添加权限，默认没有权限",
    delete_operate int default 0 comment "删除权限，默认没有权限",
    update_operate int default 0 comment "更新权限，默认没有权限",
    desc_operate int default 0 comment "查看权限，默认没有权限",
    permission_range varchar(50) comment "范围权限",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;



 create table jckk_leads (
    id int primary key auto_increment,
    uid int comment "线索负责人",
    company_name varchar(100) comment "公司名称",
    lead_from varchar(100)  comment "线索来源",
    contact_id int  comment "联系人id",
    address varchar(200)  comment "地址",
    contact_time int  comment "下次联系时间",
    contact_content text comment "下次联系内容",
    note text comment "备注",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;



 create table jckk_opportunity (
    id int primary key auto_increment,
    uid int  comment "负责人id",
    customer_id int comment "客户id",
    opportunity_name varchar(100) comment "商机名称",
    opportunity_from varchar(100)  comment "来源",
    contact_id int  comment "联系人id",
    address varchar(200)  comment "地址",
    contact_time int  comment "下次联系时间",
    contact_content text comment "下次联系内容",
    note text comment "备注",
    money varchar(50)  comment "商机金额",
    type varchar(50)  comment "类型",
    status varchar(50)  comment "状态",
    gain_rate varchar(50)  comment "赢单率",
    plan_money varchar(50)  comment "预计成本价",
    product_id varchar(50)  comment "产品id",
    forward_time int  comment "上次推进时间",
    is_delete int default 0 comment "是否删除",
    create_uid int comment "创建者id",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;


 create table jckk_product (
    id int primary key auto_increment,
    product_name varchar(100) comment "产品名称",
    product_from varchar(100)  comment "开发团队",
    type varchar(50)  comment "产品类型",
    cost_price varchar(50)  comment "成本价",
    recommend_price varchar(50)  comment "建议零售价",
    link varchar(50)  comment "详情链接",
    img varchar(200)  comment "产品图片",
    develop_time varchar(50)  comment "研发时间",
    note text comment "备注",
    is_delete int default 0 comment "是否删除",
    create_uid int comment "创建者id",
    create_time int comment "加入时间"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;



 create table jckk_task (
    id int primary key auto_increment,
    project_id int comment "项目id",
    task_type varchar(100)  comment "任务类型",
    to_uid int  comment "指派给",
    message_id int  comment "系统消息",
    is_send_mail int default 0  comment "是否发邮件提醒",
    task_name varchar(50)  comment "任务名称",
    task_describe text  comment "任务描述",
    audit_standard text  comment "审核标准",
    create_uid int comment "创建者",
    task_start varchar(50) comment "开始时间",
    task_end varchar(50) comment "结束时间",
    task_order int comment "优先级",
    file varchar(100) comment "附件",
    task_status int default 0 comment "任务状态",
    actual_start varchar(50) comment "实际开始",
    actual_end varchar(50) comment "实际完成",
    complete_file varchar(100) comment "完成附件",
    create_time int comment "加入时间",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;


 create table jckk_task_history (
    id int primary key auto_increment,
    task_id int comment "任务id",
    title varchar(100) comment "标题",
    before_value varchar(200) comment "之前值",
    after_value varchar(200) comment "之后值",
    create_uid int comment "创建者",
    create_time int comment "加入时间"

    )ENGINE=MYISAM DEFAULT CHARSET=utf8;

create table jckk_task_time (
    id int primary key auto_increment,
    task_id int comment "任务id",
    title varchar(100) comment "标题",
    note text comment "备注",
    file varchar(200) comment "附件",
    start_time varchar(50) comment "开始时间",
    end_time varchar(50) comment "结束时间",
    create_uid int comment "创建者",
    create_time int comment "加入时间"

    )ENGINE=MYISAM DEFAULT CHARSET=utf8;

create table jckk_message (
    id int primary key auto_increment,
    from_uid int comment "发信者",
    to_uid int comment "收信者",
    title varchar(200) comment "标题",
    content text comment "内容",
    status int default 0 comment "状态，0未读，1已读",
    type varchar(50)  comment "类型",
    create_uid int comment "创建者",
    create_time int comment "加入时间",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;




create table jckk_question (
    id int primary key auto_increment,
    question_type varchar(50) comment "类型",
    title varchar(100) comment "标题",
    create_time int comment "加入时间",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;

create table jckk_answer (
    id int primary key auto_increment,
    question_id int comment "问题",
    content varchar(200) comment "内容",
    sort varchar(10) comment "序号",
    score int comment "分数",
    create_time int comment "加入时间",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;


create table jckk_theme (
    id int primary key auto_increment,
    theme_type varchar(30) comment "类型",
    theme_name varchar(100) comment "主题名称",
    create_time int comment "加入时间",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;

create table jckk_theme_describe (
    id int primary key auto_increment,
    theme_id int comment "主题",
    theme_describe varchar(200) comment "内容",
    describe_sort int comment "所属行号",
    create_time int comment "加入时间",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;


create table jckk_leave (
    id int primary key auto_increment,
    uid int comment "请假者",
    department_id int comment "部门",
    post_id int comment "岗位",
    leave_type varchar(50) comment "请假类别",
    leave_start varchar(50) comment "开始时间",
    leave_end varchar(50) comment "结束时间",
    work_day varchar(20) comment "请假几个工作日",
    leave_reason varchar(200) comment "请假事由",
    audit_status int  default 0 comment "审核状态",
    create_time int  comment "创建时间",
    create_uid int  comment "创建者",
    is_delete int default 0 comment "是否删除"
    )ENGINE=MYISAM DEFAULT CHARSET=utf8;
